# ============================================
# X-HOST BACKEND CONFIGURATION
# ============================================
# Copy this file to .env and update with your actual values
# NEVER commit the .env file to version control!

# ============================================
# MONGODB CONNECTION
# ============================================
# Get this from your MongoDB Atlas dashboard:
# 1. Go to MongoDB Atlas (https://cloud.mongodb.com)
# 2. Click "Connect" on your cluster
# 3. Choose "Connect your application"
# 4. Copy the connection string and replace <password> with your actual password
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# ============================================
# SERVER PORT (X-HOST FIXED PORT)
# ============================================
# X-Host uses a fixed port that cannot be changed
# This is the port assigned by your x-host server
PORT=25539

# ============================================
# ENVIRONMENT
# ============================================
# Set to 'production' for live deployment
NODE_ENV=production

# ============================================
# AUTO-SEED DATABASE
# ============================================
# Set to 'true' to automatically seed events and news on first run
# Set to 'false' after initial setup
AUTO_SEED=true

# ============================================
# ADMIN PASSWORD
# ============================================
# IMPORTANT: Change this to a strong password!
# This is used for admin authentication
ADMIN_PASSWORD=change-this-to-a-secure-password

# ============================================
# JWT SECRET
# ============================================
# Generate a secure random string for JWT token signing
# Generate with: openssl rand -hex 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=generate-a-very-long-random-secure-string-minimum-32-characters

# ============================================
# DEPLOYMENT NOTES
# ============================================
# This backend will be deployed to X-Host (port 25539)
# Frontend will be deployed separately to Netlify
# Netlify will connect to this backend via API calls

# If you deploy the backend behind a panel or proxy (for example: panel.arabdevs.xyz/server/aa10fd22)
# you can set a public base url here. This value is optional and only used for documentation / scripts.
# Example: https://panel.arabdevs.xyz/server/aa10fd22
PUBLIC_BASE_URL=https://panel.arabdevs.xyz/server/aa10fd22

# If your hosting platform maps a different internal port, update PORT accordingly.
# Make sure the portal/panel routing maps requests to the same PORT.
